// Console Override - DISABLED to prevent infinite loop
// The logger now uses original console methods internally to avoid circular dependency
console.log('🔧 Console override disabled - logger uses original console methods to prevent infinite loop');

export {};
